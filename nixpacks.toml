[phases.setup]
aptPkgs = ["tesseract-ocr", "tesseract-ocr-ara", "tesseract-ocr-tur", "libgl1"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port $PORT"
